export default function SettingsPage() {
  return <div className="glass p-4">Settings panel includes fully-processed mapping, follow-up timings, templates, payment threshold, auto-send, and role permissions.</div>;
}
