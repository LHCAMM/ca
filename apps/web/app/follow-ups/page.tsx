export default function FollowUpsPage() {
  return (
    <main className="space-y-2">
      <h1 className="text-xl font-semibold">Follow Ups</h1>
      <div className="glass p-4 text-sm">Queue, schedule, draft viewer, and auto-send toggle (default OFF) are controlled by API settings endpoints.</div>
    </main>
  );
}
